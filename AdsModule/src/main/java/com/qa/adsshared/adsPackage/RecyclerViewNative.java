package com.qa.adsshared.adsPackage;

import com.google.android.gms.ads.AdLoader;

public class RecyclerViewNative {

/*    public void preloadAds(int numberOfAds) {
        AdLoader.Builder adLoaderBuilder = new AdLoader.Builder(context, "YOUR_AD_UNIT_ID");
        for (int i = 0; i < numberOfAds; i++) {
            adLoaderBuilder.forNativeAd(new NativeAd.OnNativeAdLoadedListener() {
                @Override
                public void onNativeAdLoaded(NativeAd nativeAd) {
                    adList.add(nativeAd);
                    notifyDataSetChanged();
                }
            });
        }
        adLoaderBuilder.build().loadAds(new AdRequest.Builder().build(), numberOfAds);
    }*/
}
